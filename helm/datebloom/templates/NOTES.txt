Datebloom is deployed 🎉

{{- if .Values.ingress.enabled }}

It will be reachable at:
{{- range .Values.ingress.hosts }}
  https://{{ .host }}
{{- end }}

DNS: point the above host(s) at your ingress controller's external IP/LB.
TLS: a certificate ({{ (index .Values.ingress.tls 0).secretName }}) is requested via the
cert-manager issuer in the ingress annotations — make sure that ClusterIssuer exists.
{{- else }}

Ingress is disabled. Port-forward to test locally:
  kubectl port-forward svc/{{ include "datebloom.fullname" . }} 8080:{{ .Values.service.port }}
  open http://localhost:8080
{{- end }}

Image in use: {{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}
